{
  "name": "relatorio-cameras",
  "version": "1.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node --openssl-legacy-provider server.js",
    "start": "node --openssl-legacy-provider server.js",
    "ui": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "net-snmp": "^3.26.3",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "vite": "^8.1.4"
  }
}
